#!/bin/bash

# rewcon
cp rewcon.sh /usr/local/bin/rewcon
chmod +x /usr/local/bin/rewcon

# easy-bust
cp easy-bust.sh /usr/local/bin/easy-bust
chmod +x /usr/local/bin/easy-bust

# set-myip
cp set-myip.sh /usr/local/bin/set-myip
chmod +x /usr/local/bin/set-myip

# start-webserver
cp start-webserver.sh /usr/local/bin/start-webserver
chmod +x /usr/local/bin/start-webserver